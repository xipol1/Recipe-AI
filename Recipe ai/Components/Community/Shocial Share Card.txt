import React from "react";
import { Badge } from "@/components/ui/badge";
import { Clock, Users, Star, Sparkles } from "lucide-react";
import DespensaLogo from "../ui/DespensaLogo";

export default function SocialShareCard({ recipe }) {
  if (!recipe) return null;

  return (
    <div className="w-full max-w-2xl mx-auto bg-gradient-to-br from-white to-orange-50 rounded-2xl shadow-2xl overflow-hidden border border-orange-200">
      {/* Header con branding */}
      <div className="bg-gradient-to-r from-emerald-500 to-green-600 p-4">
        <div className="flex items-center justify-between text-white">
          <DespensaLogo className="w-10 h-10" showText={true} textClassName="text-white" />
          <Badge className="bg-white/20 text-white border-white/30">
            Receta Compartida
          </Badge>
        </div>
      </div>

      <div className="p-6">
        {/* Imagen de la receta */}
        {recipe.image_url && (
          <div className="relative h-64 rounded-xl overflow-hidden mb-6 shadow-lg">
            <img
              src={recipe.image_url}
              alt={recipe.title}
              className="w-full h-full object-cover"
            />
            <div className="absolute inset-0 bg-gradient-to-t from-black/30 to-transparent" />
            {recipe.is_ai_generated && (
              <div className="absolute top-4 right-4">
                <Badge className="bg-purple-500 text-white">
                  <Sparkles className="w-3 h-3 mr-1" />
                  IA
                </Badge>
              </div>
            )}
          </div>
        )}

        {/* Contenido de la receta */}
 